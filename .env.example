# Database Configuration (Supabase)
DATABASE_URL=postgresql://user:password@host:port/database

# Supabase Configuration
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Email Configuration (Resend)
RESEND_API_KEY=re_your_resend_api_key_here
# Use onboarding@resend.dev for testing (no DNS required)
# Or configure your own domain: breathwork@smarason.is
FROM_EMAIL=onboarding@resend.dev

# Application Configuration
NODE_ENV=development
PORT=5000

# JWT Session Secret (REQUIRED for authentication)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here-use-openssl-rand-base64-32
